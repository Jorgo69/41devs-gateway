# Copier en .env.local pour surcharger la config de la démo (docs-site).
# Sans ce fichier : l'API VEEP réelle de production est utilisée (comportement par défaut).

# Local avec le mock (npm run mock à la racine du repo) :
# VITE_API_BASE_URL=http://localhost:4000/api/v1
# VITE_DEMO_EVENT_ID=mock-event-1

# VITE_PUBLIC_KEY=
# VITE_API_BASE_URL=
# VITE_DEMO_EVENT_ID=
